import React from 'react';
import { Layout } from "../../components/layout/Layout";

const MainPage = () => {
    return (
        <Layout>
            메인페이지
        </Layout>
    );
}

export default MainPage;
